# 检查更新

Chat2DB 设计了一种自动更新机制，以确保用户始终拥有最新的软件版本。默认情况下，系统会定期检查是否有可用的新版本，并在发现更新时通知用户。

- **自动更新**：Chat2DB 使用热更新方式，这意味着您无需手动下载和安装新版本，只需接受提示即可享受最新功能。
- **网络要求**：为了启用自动更新功能，Chat2DB 需要在本地网络外具有 HTTP 访问权限。如果没有这样的权限，系统将无法检查更新并进行热更新。


!['update'](/image/settings/update.png)
